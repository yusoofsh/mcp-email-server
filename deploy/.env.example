MCP_IMAGE=ghcr.io/yusoofsh/mcp-email-server:latest
MCP_PUBLIC_URL=https://mail-mcp.example.com
MCP_AUTH_USERNAME=operator
# Use the exact redirect URL shown by one client, or set this to * to permit
# any dynamically registered client with a valid client-specific callback.
MCP_AUTH_REDIRECT_URIS=https://chatgpt.com/connector/oauth/REPLACE_WITH_CALLBACK_ID
